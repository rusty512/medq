{
  "name": "medq",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "cd backend && npm run build",
    "start": "cd backend && npm start",
    "install": "cd backend && npm install"
  },
  "description": "MedQ application"
}
